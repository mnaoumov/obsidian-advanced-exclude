[Docs](https://github.com/mnaoumov/obsidian-advanced-exclude/)

# Draft

A second archived note, so the `Archive/` folder has more than one file to hide.
Nothing links here - it is just extra content that should disappear from the Files
pane when you exclude the `Archive/` folder in [[01 Exclude a folder]].
